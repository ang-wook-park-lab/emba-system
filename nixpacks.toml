[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'cd frontend && npm ci',
  'cd frontend && npm run build',
  'cd backend && npm ci --production'
]

[start]
cmd = 'cd backend && node server.js'

